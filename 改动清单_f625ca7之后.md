# f625ca7 之后的全部改动（已整体回退）— 待你审阅是否加回本地

> **当前状态**：线上 + 本地 HEAD 都 = `f625ca7`。下面 16 个提交在刚才的强制回退后都已不在代码里。
> **规则**：你勾选要哪些 → 我们确定后我**只在本地改、不推送**。
> **红线**：打标核心逻辑（890f2c2 / 4d2303e / a7d73b2 的打标部分 / bb47642）你已明确不动，清单里标 🚫，建议不加回。

---

## 🅰️ 打标核心逻辑（🚫 你已明令不动，仅列作复盘）

### [890f2c2] 修复打标输出LLM推理文本（强制JSON+推理剥离+模糊匹配）
- 改了什么：buildPrompt 对 multi/single 字段强制 JSON 格式；cleanLabel 新增 tryExtractTagJSON / stripReasoningText / fuzzyMatchOption；超长标签(>30)丢弃。
- 影响：开始把"自由文字标签"扭成 JSON 输出，是后来打标被破坏的起点。
- 我的建议：🚫 不加回

### [4d2303e] 三层根治打标推理文本（API jsonMode+few-shot+垃圾检测）
- 改了什么：callAPI 加 `response_format:{json_object}`；buildPrompt 加 few-shot + 错误示例；cleanLabel 加 isGarbageOutput 4条规则。
- 影响：`response_format` 不被你的 DeepSeek 接口支持 → **API 400，整个打标崩溃**（下一提交才修）。
- 我的建议：🚫 不加回

### [a7d73b2] 移除 response_format 修 400 + 新增 AI优化提示词（三处UI）
- 改了什么：① 移除 jsonMode 修 400（打标部分）；② **新增"AI优化提示词"功能**（dfPrompt 旁 / 字段面板 / 导入标签体系弹窗三处，调 LLM 把粗糙意图重写成语义理解型指令）。
- 打标相关：①是，②不是（独立功能）。
- 我的建议：①🚫 不打标；② ✅ **AI优化提示词功能可单独加回**（不碰打标逻辑，只是帮你写更好的 prompt）

### [bb47642] 彻底根治打标垃圾输出（8条规则+自动重试+编辑距离）
- 改了什么：isGarbageOutput 扩到 8 条（拉丁占比/数字开头/重复词/填充词/CJK纯英文等）；检测到垃圾自动温度降 0.1 + ⚠️ 重试。
- 影响：误杀正常英文/数字/重复词标签，是破坏最严重的版本。
- 我的建议：🚫 不加回

---

## 🅱️ Excel / 导入修复（✅ 实用性强，建议重点评估）

### [3597687] 修复 Excel 多工作表只识别一个 + 状态栏 0行0列
- 改了什么：parseExcel 不是真 async → await 立即返回 → 数据未解析完就渲染。改为返回 Promise + doParseExcel 真 await FileReader。
- 我的建议：✅ 加回（修复多表导入根本问题）

### [c49eca0] SheetJS CDN 加载失败 → 4个 fallback
- 改了什么：`_XLSX_FALLBACKS` 四个 CDN（jsdelivr→unpkg→cdnjs→sheetjs）自动重试；parseExcel/ensureXLSX 统一走 fallback。
- 我的建议：✅ 加回（你之前环境 cdn.sheetjs.com 访问不了，导入直接失败）

### [47e8564] 修复 td 未定义导致 Excel 解析失败
- 改了什么：`updateSjButton()` 引用了 renderTable 局部变量 `td` → ReferenceError 被 catch 吞掉 → "解析失败"。改为 `tableData().length`。
- 我的建议：✅ 加回（1 行改动，修导入崩溃）

### [cf18019] 多工作表导入加调试日志 + 标签栏视觉增强
- 改了什么：doParseExcel 加 console.log 调试；sheet-tabs CSS 改为顶部渐变 + 激活下划线。
- 我的建议：⚪ 看你（视觉增强可要可不要，调试日志可去掉）

---

## 🅲️ 字段交互（UX，非打标核心）

### [6141204] 勾选列头即弹出该列打标配置
- 改了什么：toggleColSel 勾选某列 → 已是 AI 字段则编辑、否则新建预填列名；仅唯一选中列时弹。
- 我的建议：⚪ 看你（交互便利，不碰打标逻辑）

### [f2e1314] 勾选AI字段列头改为滚动到侧边栏字段卡片（不再弹抽屉）
- 改了什么：勾选 AI 字段 → scrollToFieldCard 滚动高亮；非 AI 字段 → 仍弹新建。
- 我的建议：⚪ 看你（纯交互，之前你要求的行为）

---

## 🅳️ 翻译功能

### [01a6fc4] 新增翻译字段类型 translate + 提示词触发翻译
- 改了什么：字段类型加"翻译"；prompt 含"翻译/译成"自动识别；buildPrompt 翻译分支走专业翻译提示词。
- 我的建议：⚪ 看你（要不要翻译字段）

### [506d955] 新增免费 Google 翻译接口（可切换后端）
- 改了什么：配置面板加"翻译引擎"选择器（DeepSeek / Google 免Key）；callGoogleTranslate 调免Key端点。
- 我的建议：⚪ 看你（配合 01a6fc4，免Key翻译很实用）

---

## 🅴️ AI 助手

### [cc21a7f] AI助手新增筛选建表 filterSheet
- 改了什么：按条件筛选当前工作表行 → 新建独立工作表；rowMatchesFilter 支持 between/gte/contains 等算子。
- 我的建议：⚪ 看你（AI 对话里建子表，按需）

---

## 🅵️ 视觉 / 滚动条

### [e188740] 滚动跳转按钮改为底部居中 + 智能切换上下箭头
- 改了什么：位置底部居中、单按钮智能切换↑↓、smooth 动画、不可滚动时隐藏。
- 我的建议：⚪ 看你

### [a7df158] 表格区域纵向滚动条加宽可见（12px+渐变）
- 改了什么：滚动条样式加宽。
- 我的建议：⚪ 看你

### [9cc342b] 隐藏列留可见标记可一键恢复 + 列拖拽多选边缘自动横向滚动
- 改了什么：隐藏列后在网格留斜纹「N‹」标记一点恢复；拖拽多选光标靠边自动横向滚动。
- 我的建议：✅ 建议加回（隐藏列后找不到恢复入口、拖拽选不全的痛点很实际）

---

## 📋 你的决定栏（回复我勾哪些，或直接说"加回 B 全部 + 9cc342b + a7d73b2的AI优化提示词"）

- [ ] 3597687 多工作表修复
- [ ] c49eca0 SheetJS fallback
- [ ] 47e8564 td 修复
- [ ] cf18019 标签栏增强
- [ ] 6141204 列头弹配置
- [ ] f2e1314 侧边栏卡片
- [ ] 01a6fc4 翻译字段
- [ ] 506d955 Google 翻译
- [ ] cc21a7f 筛选建表
- [ ] e188740 滚动跳转按钮
- [ ] a7df158 滚动条加宽
- [ ] 9cc342b 隐藏列标记
- [ ] a7d73b2 的 AI优化提示词功能（不打标部分）
- [ ] 🚫 打标核心（890f2c2/4d2303e/bb47642）—— 默认不加

> 确定后我**只在本地改、不推送**，改完发你本地预览验证。
